apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/kal2t/jxtest2:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/kal2t/jxtest2:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: jxtest2
        chartPath: charts/jxtest2
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/kal2t/jxtest2'
          image.tag: '{{.DIGEST_HEX}}'
